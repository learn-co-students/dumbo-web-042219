

1. When X event happens
    DOMContentLoaded
2. I want to make Y fetch
    GET http://localhost:3000/quotes
3. And then slap Z on (or off) the DOM
    Put the LI from the readme on the DOM






1. When X event happens
    click on the like button
2. I want to make Y fetch
    `POST` request to `http://localhost:3000/likes` with { quoteId: 5 }
3. And then slap Z on (or off) the DOM
  increment the displayed number of likes










